<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Sharing App</title>
  </head>
  <body
    style="
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue',
        sans-serif;
      color: #f5f5f5;
      background-color: #222;
    "
  >
    <% if (locals.fileLink != null) { %>
    <div style="margin: 1rem auto">
      Your file has been uploaded:
      <a href="<%= locals.fileLink %>"><%= locals.fileLink %></a>
    </div>
    <% } %>
    <form
      method="post"
      action="/upload"
      enctype="multipart/form-data"
      style="display: grid; gap: 0.5rem; grid-template-columns: auto 1fr; max-width: 500px; margin: 0 auto"
    >
      <label for="file">File:</label>
      <input type="file" id="file" name="file" required />
      <label for="password">Password:</label>
      <input type="password" id="password" name="password" />
      <button style="grid-column: span 2; padding: 5px 10px; margin-top: 0.5rem" type="submit">Share</button>
    </form>
  </body>
</html>
